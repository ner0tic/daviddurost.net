{#  src/DdnetUtilityBundle/Resources/views/Default/contact.html.twig #}
{% extends '::base.html.twig' %}
{% block stylesheets %}
  {{ parent() }}
  <link rel="Stylesheet" href="{{ asset('/css/contact.css.php') }}" />
  <link rel="Stylesheet" href="{{ asset('/css/foursquare.css.php') }}" />
{% endblock %}
{% block content %}
<div>
  <div class="contact-social-wrapper form-wrapper" id="contact-social-pane">
  {% block contact_form %}
    <div class="main-column" id="contact">
      <h2>{{ 'contact me' | title }}</h2>
      <form action="{{ path('contact') }}" method="post" {{ form_enctype(form) }} id="contact-form">
        {{ form_errors(form) }}
        <div class="slider">
          {{ form_label(form.name) }}
          {{ form_widget(form.name) }}
        </div>
        <div class="slider">
          {{ form_label(form.email_address) }}
          {{ form_widget(form.email_address) }}
        </div>
        <div class="slider">
          {{ form_label(form.url) }}          
          {{ form_widget(form.url) }}
        </div>
        <div class="slider">
          {{ form_label(form.topic) }}
          {{ form_widget(form.topic) }}  
        </div>
        <div class="slider">
          {{ form_label(form.message) }}
          {{ form_widget(form.message) }}
        </div>
        <div>
        {{ form_rest(form) }}
        <input type="submit" class="submit greenButton" id="contactSubmit" value="go go gadget" />
        </div>
      </form>
      {% include 'DdnetUtilityBundle:Default:socialbar.html.twig' with { 'page' : page } %}
    </div>
  {% endblock %}      
  </div>
  <div class="contact-social-wrapper foursquare-wrapper" id="contact-4sq-pane">
    {% block foursquare %}  
    {% include 'DdnetUtilityBundle:Default:4sq.html.twig' with { 'checkin' : checkin, 'venue' : venue, 'map' : map } %}
    {% endblock %}
  </div>
</div>
{% endblock %}
{% block javascripts %}
  {{ parent() }}
  {{ google_map_js(map) }}
{% endblock %}  